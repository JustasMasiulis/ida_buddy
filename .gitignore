__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/
.claude
.tmp

# IDA databases (generated). Fixture binaries are tracked via the negations below.
*.i64
*.idb
*.id0
*.id1
*.id2
*.nam
*.til
# Ignore fixture *contents* (not the dir) so specific binaries can be re-included.
tests/fixtures/*
!tests/fixtures/hvix64.exe
